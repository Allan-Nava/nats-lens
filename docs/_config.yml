title: NATS Lens
description: Client NATS dentro VS Code — context CLI, publish/subscribe/request, browser JetStream.

# Layout custom (docs/_layouts/default.html) applicato a tutte le pagine.
defaults:
  - scope:
      path: ""
    values:
      layout: default

exclude:
  - "*.vsix"
